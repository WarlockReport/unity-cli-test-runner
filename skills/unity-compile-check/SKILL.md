---
name: unity-compile-check
description: Unity CLI（`~/.unity/bin/unity cmd`、Pipelineサーバー経由で起動中エディタに接続）を使い、テストは実行せずコンパイルエラーの有無だけを素早く確認する。コード修正直後に「テストはいいのでコンパイル通るか確認して」「ビルドが通るか確認して」のように、テスト実行を伴わない検証を求められたときに使う。実装サブエージェントを含め、Bashツールが使える主体なら誰でも直接使ってよい（`unity-cli-test-runner`スキルと異なり、コントローラー本体・専用サブエージェントへの利用制限は無い）。テストのPass/Fail確認が必要な場合はこのスキルではなく`unity-cli-test-runner`スキルを使うこと。
---

# Unity コンパイル確認（テスト実行なし）

`unity cmd` を使い、コード修正後にコンパイルエラーの有無だけを確認する。テスト対象の解決・実行は
一切行わない。

**このスキルはテスト実行を伴わないため、`unity-cli-test-runner`スキルと違って利用主体を限定しない。
実装サブエージェント自身がコード修正の直後に直接呼び出してよい。**

## 前提

- 各コマンドには必ず明示的な `--timeout <秒>` を付ける（内部で呼ぶスクリプト側が既定値を持つ）
- 本ドキュメント中の `scripts/...` は、`unity-cli-test-runner` スキルのディレクトリ配下にある
  スクリプトを相対パス `../unity-cli-test-runner/scripts/...` で参照する（ロジックの複製を避けるため。
  実行時はこのスキルのベースディレクトリを起点に絶対パスへ解決してから呼び出すこと）

## ワークフロー

```
- [ ] 0. エディタ起動確認
- [ ] 1. コンパイル状態の確定
- [ ] 2. 結果報告
```

### 0. エディタ起動確認

```bash
../unity-cli-test-runner/scripts/check-editor-ready.sh
```

| 終了コード | 意味 | 対応 |
| --- | --- | --- |
| 0 | 起動中エディタが見つかり status が ready | 次のステップへ進む |
| 1 | 起動中エディタは見つかったが status が ready 以外 | 標準エラー出力のstatus値をそのまま報告する |
| 2 | 起動中エディタが見つからない、リトライ予算を使い切っても失敗/タイムアウト | Unityエディタが起動していないか未接続。呼び出し元（人間またはコントローラー）に手動起動・接続確認を依頼する |
| 3 | 応答は得られたが想定した形式でパースできなかった | 標準出力の生JSONを確認して手動判断する |

### 1. コンパイル状態の確定

```bash
../unity-cli-test-runner/scripts/ensure-compile-clean.sh [予算秒数(既定60)]
```

`clear_console → recompile → recompile_status ポーリング → editor_status によるドメインリロード
完了の安定確認 → get_console_logs(error)` を順に実行する（詳細・自動リカバリの挙動は
`unity-cli-test-runner` スキルの該当箇所を参照。ロジックは完全に共通）。

| 終了コード | 意味 | 対応 |
| --- | --- | --- |
| 0 | コンパイル確定・ドメインリロードも完了・エラー無し | 次のステップ（結果報告）へ進む |
| 1 | コンパイルエラーを検出（標準出力に詳細） | エラー内容をそのまま報告して終了する |
| 2 | ポーリング予算超過（`unity cmd`呼び出し自体の失敗を含む。スクリプト内自動リカバリを試みても解消しなかった） | 盲目的に再試行せず、呼び出し元にハング・タイムアウトの可能性をそのまま報告する |
| 3 | `get_console_logs` の返り値の形式が想定と異なり判定不能 | 標準出力の生JSONを確認して手動判断する |

複数ファイルにまたがる大きめの修正の直後はドメインリロードが長引く傾向があるため、既定を
待たずに第1引数で予算を伸ばしてよい（例: `ensure-compile-clean.sh 120`）。

### 2. 結果報告

- 終了コード0: 「コンパイルエラー無し」と簡潔に報告する
- 終了コード1: 検出したエラー件数と詳細（`get_console_logs` の出力）をそのまま報告する
- 終了コード2: ハング・タイムアウトの可能性を報告し、同じ呼び出しを盲目的に再試行しない
- 終了コード3: 生JSONを添えて手動判断を仰ぐ

## 既知の罠

コンパイル完了直後・ドメインリロード中の一時的なPipeline切断など、`unity-cli-test-runner` スキルの
「既知の罠」に記載された注意点は `ensure-compile-clean.sh` 内部でそのまま吸収される（ロジック共通の
ため、このスキル固有の追加事項は無い）。

## 禁止事項

- このスキルの範囲はコンパイル確認のみ。テスト対象の解決・実行が必要になった場合は、このスキルを
  拡張して代用せず、`unity-cli-test-runner` スキル（コントローラー本体または専用の
  `unity-test-runner` サブエージェント経由）に切り替える
- タイムアウト・ハング時に原因を潰さないまま盲目的に再試行しない
